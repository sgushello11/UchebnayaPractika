<Window x:Class="HranitelProDivision.AddToBlacklistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Черный список" Height="481" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <StackPanel Margin="20">
        <TextBlock Text="🚫 Работа с черным списком" FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>

        <TextBlock Text="ФИО:" FontWeight="Bold"/>
        <TextBlock x:Name="lblFullName" Margin="0,0,0,10"/>

        <TextBlock Text="Паспортные данные:" FontWeight="Bold"/>
        <TextBlock x:Name="lblPassport" Margin="0,0,0,15"/>

        <!-- Блок для добавления -->
        <Border x:Name="borderAdd" BorderBrush="DarkRed" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="➕ Добавление в черный список" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Причина добавления:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtReason" Height="100" TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>
                <Button x:Name="btnAdd" Content="Добавить в черный список" Background="DarkRed" Foreground="White" Height="35"/>
            </StackPanel>
        </Border>

        <!-- Блок для удаления -->
        <Border x:Name="borderRemove" BorderBrush="DarkGreen" BorderThickness="1" Padding="10" Margin="0,0,0,10" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="❌ Удаление из черного списка" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Причина добавления:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="lblExistingReason" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="DarkRed"/>
                <Button x:Name="btnRemove" Content="Удалить из черного списка" Background="DarkGreen" Foreground="White" Height="35"/>
            </StackPanel>
        </Border>

        <Button x:Name="btnClose" Content="Закрыть" Height="35" Margin="0,5"/>

        <TextBlock x:Name="lblError" Foreground="Red" Margin="0,5" TextAlignment="Center"/>
    </StackPanel>
</Window>